import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    backgroundColor: '#0D0D0D',
    paddingTop: 80,
    paddingBottom: 60,
  },
  image: {
    width: 110.34,
    height: 32,
  },
});
